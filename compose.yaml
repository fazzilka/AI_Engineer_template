services:
  app:
    build:
      context: .
    env_file:
      - .env
    ports:
      - "8000:8000"
    init: true
    restart: unless-stopped
    read_only: true
    tmpfs:
      - /tmp
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
